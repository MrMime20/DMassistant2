{
  "name": "dm-assistant",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "lint": "ls -la /workspace"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
